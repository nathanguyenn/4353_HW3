
<head>
    <title>Fuel Quote Form</title>
    <link rel="stylesheet" href="./css/signup.css">
    <script src="https://kit.fontawesome.com/afb61f74f2.js" crossorigin="anonymous"></script>
</head>

<body>
    <form action="/fuelQuoteForm" style="border:1px solid #ccc" method="POST">
        <div class="container">
        <h1>Fuel Quote Form<i onclick="location.href='/profile'" class='far fa-user-circle'></i></h1>
        <p>Please fill out the following fields to recieve your quote.</p>
        <hr>
    
        <label for="galReq"><b>Gallons Requested</b></label>
        <br><br>
        <input type="number" step="0.01" value="<%=gallonsRequested %>" name="galReq" min="0" required>
        <br><br>
        <label for="delivAdd"><b>Delivery Address</b></label>
        <input type="text" value="<%= current_user.street + ", " + current_user.city +", " +current_user.state+ " " +current_user.zip%>"
               placeholder="<%= current_user.street + ", " + current_user.city +", " +current_user.state+ " " +current_user.zip%>" name="delivAdd" readonly>

        <label for="delivDate"><b>Delivery Date</b></label>
        <br> <br> 
        <input type ="date" id="datefield" value="<%=deliveryDate%> "  name="delivDate" min="2022-02-05" required>
        <br>  <br> 
        <p>If address is incorrect please fix on <a href="/profile" style="color:dodgerblue">User Profile</a>.</p>

            <br> <br> 
        <label for="suggestedPrice"><b>Suggested Price / gallon:</b></label>
        <input type="text" placeholder="$<%= customerPricePerGallon %>" name="suggestedPrice" readonly>

        <label for="totalAmntDue"><b>Total Amount Due</b></label>
        <input type="text" placeholder="$<%= totalPrice %>" name="totalAmntDue" readonly>
    
        <div class="clearfix">
            <button type="button" class="cancelbtn" onclick="location.href='/history'">Fuel Quote History</button>
            <button type="submit" class="signupbtn" name="submit" value="submit">Submit Form</button>
            <button type="submit" class="signupbtn" name="getPrice" value="getPrice" >Get Quote</button>
        </div>
    </form>
</body>
<script>
    var today = new Date();
var dd = today.getDate();
var mm = today.getMonth() + 1; //January is 0!
var yyyy = today.getFullYear();

if (dd < 10) {
   dd = '0' + dd;
}

if (mm < 10) {
   mm = '0' + mm;
} 
    
today = yyyy + '-' + mm + '-' + dd;
console.log(today)
document.getElementById("datefield").setAttribute("min", today);


</script>